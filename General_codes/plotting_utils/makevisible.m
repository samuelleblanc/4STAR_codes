% Simple function to make a figure that was saved with visible off, to turn
% it on

function makevisible(file)

%f=load(file,'-mat');
%n=fieldnames(f);
%f.(n{1}).properties.Visible='on';
%save(file,'-struct','f')
end
